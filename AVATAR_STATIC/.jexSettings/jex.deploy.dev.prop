################################################################################
# 배포 우선순위 형상관리등을 통해 여러 파일이 배포될때 높은 숫자를 가진 것부터 배포 된돠.
################################################################################
PRIORITY=0

################################################################################
# 빌드 타입(선언하지 않을경우 default 사용)
################################################################################
#BUILD=JSPC
#PRE_HOOKS=test.PreHookTest
#POST_HOOKS=test.PostHookTest

################################################################################
# 개발서버 1번 was 중복되지 않는 KEY 값이면 됨
################################################################################
[DEV]
################################################################################
# 서버의 연결정보 IP:Port 순서
################################################################################
HOST=10.254.242.193:9995

################################################################################
# 서버에 빌드 타입이 따로 지정해야 하는경우
################################################################################
#BUILD=JSPC

################################################################################
# 복사 COPY??? 의 순서로 시작하며 중복되면 안됨
# 선언순서) 복사방법:복사위치
# 복사방법 : 받는쪽에 jex/cm/copyInfo 하위에 선언된 Tag
# 복사위치 : 복사를 할 위치
################################################################################
COPY0=WEB:/WAS_DATA/webRoot/avatar/src

################################################################################
# 복사후 Touch 해야 하는파일이 있을경우 COPY?? 와 같이 중복되지 않게 선언
################################################################################
#TOUCH0=/???/???
